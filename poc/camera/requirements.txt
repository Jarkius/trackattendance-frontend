# Attendance System Requirements
# Install with: pip install -r requirements.txt
# Recommended Python: 3.11 or 3.12 (tested on 3.11-3.14)

# Camera and Barcode Scanning
opencv-python-headless>=4.8.0  # Headless — avoids SDL2 conflict with pygame
pyzbar>=0.1.9                  # Windows: also install zbar DLL (see README)
numpy>=1.24.0

# Voice Feedback
pygame>=2.5.0
gTTS>=2.4.0                    # Google Text-to-Speech (requires internet)
pyttsx3>=2.90                  # Offline TTS fallback (Windows SAPI5, macOS NSSpeech)

# USB Scanner (Linux only)
evdev>=1.6.1; sys_platform == 'linux'

# Alternative keyboard input
pynput>=1.7.6

# Web Dashboard
flask>=2.3.0
flask-cors>=4.0.0

# Person Detection (proximity greeting via face/pose recognition)
mediapipe>=0.10.20              # Optional: face+pose detection (falls back to motion if missing)

# Utilities
python-dateutil>=2.8.2

# ============================================================
# Platform Notes
# ============================================================
# Windows:
#   - pyzbar requires zbar DLL. Install via:
#     pip install pyzbar[scripts]
#     OR download from: https://github.com/nicedec/libzbar-windows
#   - pyttsx3 uses Windows SAPI5 for offline TTS
#   - Python 3.11-3.12 recommended for best wheel availability
#
# macOS:
#   - pyzbar requires zbar: brew install zbar
#   - Camera needs permission grant (run: python grant_camera.py)
#   - pyttsx3 uses NSSpeechSynthesizer
#
# Linux:
#   - pyzbar requires zbar: apt install libzbar0
#   - evdev for direct USB scanner access
# ============================================================
